/**
 * 版权所有：浙江薪福多网络科技有限公司
 * 作者：Tony
 * 创建时间：2017/7/31
 */

import Common from '../components/Common/Route'

let merge = (...args) => Array.prototype.concat.apply([], args);

export default merge(
    Common,
)
